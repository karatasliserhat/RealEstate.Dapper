@model List<ResultAboutServiceViewModel>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/AdminLayaout/Index.cshtml";
    int count = 0;
}

<div class="container-fluid pt-4 px-4">
    <div class="row g-4">
        <div class="col-12">
            <div class="bg-light rounded h-100 p-4">
                <h6 class="mb-4">Hizmetler</h6>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th scope="col">#</th>
                                <th scope="col">Hizmet Adı</th>
                                <th scope="col">Durumu</th>
                                <th scope="col">Güncelle</th>
                                <th scope="col">Sil</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (Model.Count > 0)
                            {
                                @foreach (var item in Model)
                                {
                                    count++;
                                    <tr>
                                        <th scope="row">@count</th>
                                        <td>@item.AboutServiceName</td>
                                        <td>@(item.AboutServiceStatus==true?"Aktif":"Pasif")</td>
                                        <td><a asp-action="Update" asp-controller="AdminAboutService" asp-route-dataId="@item.DataProtect" class="btn btn-outline-warning">Güncelle</a></td>
                                        <td><a asp-action="Delete" asp-controller="AdminAboutService" asp-route-dataId="@item.DataProtect" class="btn btn-outline-danger">Sil</a></td>
                                    </tr>
                                }
                            }
                            else
                            {
                                <tr>
                                    <td colspan="5">Hizmetler  ile ilgili kayıtlı veri yok</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <a asp-action="Create" asp-controller="AdminAboutService" class="btn btn-outline-info">Yeni Hakkımızda Girişi</a>
                </div>
            </div>
        </div>
    </div>
</div>

