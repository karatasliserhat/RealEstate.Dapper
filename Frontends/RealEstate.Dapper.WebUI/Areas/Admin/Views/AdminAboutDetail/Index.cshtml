@model List<ResultAboutDetailViewModel>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/AdminLayaout/Index.cshtml";
    int count = 0;
}

<div class="container-fluid pt-4 px-4">
    <div class="row g-4">
        <div class="col-12">
            <div class="bg-light rounded h-100 p-4">
                <h6 class="mb-4">Biz Kimiz</h6>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th scope="col">#</th>
                                <th scope="col">Başlık</th>
                                <th scope="col">Alt Başlık</th>
                                <th scope="col">Açıklama</th>
                                <th scope="col">Açıklama2</th>
                                <th scope="col">Güncelle</th>
                                <th scope="col">Sil</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (Model.Count > 0)
                            {
                                @foreach (var item in Model)
                                {
                                    count++;
                                    <tr>
                                        <th scope="row">@count</th>
                                        <td>@item.Title</td>
                                        <td>@item.SubTitle</td>
                                        <td>@item.Description</td>
                                        <td>@item.Description2</td>
                                        <td><a asp-action="Update" asp-controller="AdminAboutDetail" asp-route-dataId="@item.DataProtect" class="btn btn-outline-warning">Güncelle</a></td>
                                        <td><a asp-action="Delete" asp-controller="AdminAboutDetail" asp-route-dataId="@item.DataProtect" class="btn btn-outline-danger">Sil</a></td>
                                    </tr>
                                }
                            }
                            else
                            {
                                <tr>
                                    <td colspan="7">Hakkımızda Bilgisi ile ilgili kayıtlı veri yok</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <a asp-action="Create" asp-controller="AdminAboutDetail" class="btn btn-outline-info">Yeni Hakkımızda Girişi</a>
                </div>
            </div>
        </div>
    </div>
</div>

